<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
  <div class="container-fluid" style="font-size: 17px;">
    <ul class="navbar-nav">
        {{#if isAuthenticated}}
            {{#if user.is_admin}}
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page"  href="/manageorders">Активные заказы</a>
                </li>
            {{else}}
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/myorders">Мои заказы</a>
                </li>
            {{/if}}
        {{else}}
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/">Главная</a>
            </li>
        {{/if}}
    </ul>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse justify-content-end" id="navbarNav">
      <ul class="navbar-nav">
        {{#if isAuthenticated}}
            {{#if user.is_admin}}
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/ordersarchive">Архив заказов</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="/dashboard">База пользователей</a>
                </li>
            {{/if}}
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/logout">Выход</a>
            </li>
        {{else}}
        <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/login">Вход</a>
            </li> 
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="/register">Регистрация</a>
            </li>             
        {{/if}}
      </ul>
    </div>
  </div>
</nav>
